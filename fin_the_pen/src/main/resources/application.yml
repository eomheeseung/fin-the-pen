# h2 driver ??
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.url=jdbc:h2:tcp://localhost/~/fin_the_pen
#spring.datasource.driver-class-name=org.h2.Driver
#spring.jpa.database=h2

#h2 DB
spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:tcp://localhost/~/fin_the_pen
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  security:
    user:
      password: 1


# mysql DB
#spring:
#  jpa:
#    database: mysql
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true
#    show-sql: true
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: fomuller@1350
#    url: jdbc:mysql://localhost:3306/fin_the_pen?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul

# Swagger springdoc-ui Configuration
springdoc:
  packages-to-scan: project.fin_the_pen
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: doc.html            # Swagger UI 경로 => localhost:8000/doc.html
    tags-sorter: alpha            # alpha: 알파벳 순 태그 정렬, method: HTTP Method 순 정렬
    operations-sorter: alpha      # alpha: 알파벳 순 태그 정렬, method: HTTP Method 순 정렬
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  cache:
    disabled: true